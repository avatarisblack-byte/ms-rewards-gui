# GUI 变更记录

GUI 控制面板（`gui/`）的变更历史。于 2026-08-20 从 `doc/CODE_MAP.md` 的「变更记录」章节原样分离而来。

**收录范围**：`gui/` 内代码/样式/页面/启动脚本的改动，以及为 GUI 而做的测试与文档同步。
仓库级、非 GUI 的改动（如 `.gitignore`、`src/`、`scripts/`）仍记录在 `doc/CODE_MAP.md`。

**相关文档**：GUI 目录结构、接口清单与关键设计决策见 `doc/CODE_MAP.md` 的「GUI 控制面板（gui/）」章节；
使用说明见 `gui/README.md`；测试套件说明见 `doc/CODE_MAP.md` 的「测试」章节。

新增条目请置于表格首行（倒序，最新在上），并写明**改了哪些文件**与**为什么这样改**。

| 日期 | 内容 |
|------|------|
| 2026-09-06 | **新功能：GUI 以独立应用窗口启动（用户需求）**：`start-gui.bat` 浏览器打开逻辑改为三级回退——Edge `--app` 模式（无标签栏/地址栏、任务栏独立图标，`--window-size=1400,900`）→ Chrome `--app` → 默认浏览器普通标签（legacy 回退）；纯 ASCII 注释遵守既有约束，块内注释用 rem（`::` 在括号块内会被当标签解析报错），含括号的 `Program Files (x86)` 路径靠引号保护（cmd 仅在引号外识别块括号）；页面新增内联 SVG favicon（奖杯），`--app` 窗口标题栏/任务栏不再显示默认地球图标；静默模式 `start-gui-silent.vbs` 跑同一 bat 自动获得新行为。实测 Edge --app 独立窗口弹出正常 |

| 2026-09-06 | **新功能：环境已安装状态提示（方案 A）**：`routes/system.js` 新增只读 `GET /api/setup/status`——实质判据实时检测（deps=node_modules/patchright、browser=patchright 浏览器缓存 chromium*（PLAYWRIGHT_BROWSERS_PATH→三平台标准路径）、build=dist/index.js，附带 envFile=.env 账号提示），比 setup.bat 留标记更可靠（手动 npm i/构建同样识别）；前端按钮双形态——installed 时切绿色 `.btn-installed`「✓ 环境已安装」（点击打开环境详情弹窗：各项 ✓/✗ + 重新运行安装程序入口），未安装保持原样；点安装后 10s 轮询直至就绪（15 分钟上限）自动切换；环境已就绪时首次打开不再弹「环境安装提示」弹窗。测试新增 I-Y06（字段结构+沙箱翻转+组合一致性），176 用例全绿 |

| 2026-09-06 | **v4 文件日志行格式适配：账号卡片/收益统计恢复实时更新（用户 newlog 实测定位）**：v4 Logger.writeLogToFile 在行首加 formatLocalTimestamp（本地时间，空格分隔+毫秒）→「YYYY-MM-DD HH:mm:ss.SSS [本地toString] [账户] [级别] 平台 [事件] 消息」——与 v3 UTC-ISO 双时间戳行、v4 console 单时间戳行均不同，parseLogLine 三种正则全部不匹配 → v4 运行段整段解析失败，账号卡片/收益停留在导入的历史日志（实测 newlog 1077 行 0 解析成功）。logger.js 新增第四分支（本地时间戳按本地时区解析回 UTC ISO，日期精确到行；注意首个括号是 toLocaleString 时间戳、账户在其后，分组曾错位产生时间戳伪账号已修正）。实测 newlog：1077 行全部解析，3 账号正确聚合（grandTotal=564） |

| 2026-09-06 | **会话迁移 + 导入计数修复（用户实测反馈）**：①v3 会话 json 落盘后 v4 仍走密码登录——v4 只读 sessions.db（SQLite），新增 `lib/sessionMigrate.js`：扫描 `<email>/session_(mobile|desktop).json`（纯 Cookie 数组）与 `session_fingerprint_*.json`，转换为 v4 的 StorageState `{cookies, origins:[]}` + fingerprint upsert 进 sessions.db（node:sqlite 零依赖，建表结构照抄 SessionStore.ts，重复迁移 upsert 不重复、指纹 COALESCE 保留）；sessions.js/data.js 导入 v3 会话后自动触发；②一键导入弹窗「账号: 1」误导——.env/accounts.json 按文件计数恒为 1，改为按解析出的实际账号数；③`.gitignore` 补 `.env` 与 `*.log`（用户运行日志含账号邮箱差点入库，推送前移出）；④resilience.test.js 清洗误入的真实控制字符。174 用例全绿（新增 U-SM1/2、I-D09/10） |
| 2026-09-06 | **v4 日志/会话格式适配：收益统计、Session 与一键导入导出在 V4-china 核心下恢复可用**（实测盘点发现的三个失配，此前验证无真实日志/会话未暴露）：①`lib/logger.js` parseLogLine 双格式——v4 Logger 只打单个 toLocaleString 本地时间戳（v3 为 UTC-ISO+本地双时间戳），原正则整行丢弃 v4 行致统计归零；v4 行 utcTime=null 由文件名（v4 仍按本地日期命名）兜底日期归属；②`lib/summary.js` 新增 parseAccountEndFields/activityGain——ACCOUNT-END 文案 v3「总计:+N、原始:X→新值:Y」/v4「获得积分=N、原余额=X、现余额=Y」双格式；活动兜底文案英文化（完成UrlReward→UrlReward 完成、获得积分=→pointsGained=）；SEARCH-BING 限定单次行（含 查询=/query=，v4 轮汇总行与单次行同积分全算双计）、READ-TO-EARN 排除汇总行同理；主进程账户字段 v3「主进程」/v4「MAIN」过滤双值；`js/app.js` parseAccountEnd 同步（最新动态关键字段）；③Session 双格式——v3 按账号 session_*.json→v4 sessions/sessions.db（SQLite+WAL），新增 `lib/sessionFiles.js` 统一收集/恢复（优先 db 文件集、回退 v3 json），`routes/sessions.js`/`routes/data.js` 导入导出接入，导入白名单放行 sessions.db/-wal/-shm；顺手移除 sessions.js 导出残留的 CORS *（0821 加固漏网点）。测试：夹具新增 v4 格式日志日（END 权威优先/汇总行不双计/MAIN 不污染/惰性兜底用独立账号 tester.c——tester.b 的 day3 段跨文件保持会按「跨天运行未完成」口径合并）与 v4 sessions.db 导入导出（I-D06/07/08，导出用解压验证内容而非字节搜索），170 用例全绿 |
| 2026-08-23 | **GUI 服务常驻改造：不再"页面断开即退出"（Edge 清后台服务根因修复）**：`gui/lib/routes/system.js` 删除「所有 SSE 保活连接断开后进入 5s 静默期、超时无新连接即 process.exit(0) 自杀」逻辑，keepalive 断开仅记日志「所有页面已断开保活连接，服务继续常驻运行」，停止方式仅剩 `/api/shutdown`（面板「关闭服务」）、`stop-gui.bat`、Ctrl+C。根因：Edge「睡眠标签页 / 内存节省器」会整体冻结后台标签页（页面 JS 定时器与网络连接全部挂起），SSE 断开后页面无法执行退避重连，5s 静默期走完服务即被"清掉"；静默启动（`start-gui-silent.vbs`）无窗口，服务退出完全无感知。`gui/js/app.js` 保活注释同步（退避重连仅用于页面冻结/网络波动后恢复时尽快重建连接，不再承担"防服务自杀"职责）。测试配套：`test/gui/resilience.test.js` R-L03 由「断开超 5s 静默期触发退出」反转为「断开后服务保持存活（常驻模式）」（等待超原静默期断言 `exitCalls` 为空），R-L01/01b/02/04/05/06 与 before 劫持注释同步更新；验证 23 用例 0 失败。文档同步：`gui/README.md`（特性表「本地运行」、停止服务章节、静默模式警示改为服务常驻说明；新增开机自启指引——`shell:startup` 放 `start-gui-silent.vbs` 快捷方式即登录自启；FAQ 新增「页面打不开 / 怎么确认服务还在运行」排查）、`doc/CODE_MAP.md`（system.js 条目、设计决策「服务常驻」、resilience 测试描述） |
| 2026-08-23 | **修复末尾账号最新动态被 RUN-END 覆盖 + 收益三口径对账说明**：`gui/lib/summary.js` 新增 run 级事件黑名单 `RUN_LEVEL_EVENTS`（RUN-START/RUN-END/CLUSTER-PRIMARY/CLUSTER-WORKER-START/TASK/EXIT/ERROR/DISCONNECT/PROCESS/MAIN-ERROR/UNCAUGHT-EXCEPTION/UNHANDLED-REJECTION）与 `isRunLevelEvent`，`summarizeLogs` 与 `generateSummary` 两处过滤在 `account==='主进程'` 之外追加按 event 跳过。根因：上游 `src/index.ts` 循环结束打印 RUN-END 时未复位动态 `userData.userName`，RUN-END 行的 [账户] 字段被污染成"最后一个账号名"而非"主进程"，此前仅按 account 过滤会把它归入末尾账号，覆盖其 `lastEvent`（前端 `parseAccountEnd` 要求 `lastEvent==='ACCOUNT-END'`，被污染后末尾账号卡片丢失自身 ACCOUNT-END、错显全局 RUN-END），并在跨天运行时把末尾账号运行段 `lastDateKey` 拖入次日导致今日收益漏计。修复后末尾账号 lastEvent 保持 ACCOUNT-END、收益段归属不被 RUN-END 污染。`gui/js/app.js` 首页收益卡副标题由"基于日志解析，含未配置账号"改为"日志解析：各账号运行段累计，含未配置账号、多次运行与未完成兜底"，准确说明今日收益与 RUN-END 总收集积分（单次余额差快照）三口径差异来源，与 `generateSummary` 计算逻辑一致。测试配套：`test/gui/unit.test.js` 新增 U-R01~04（末尾 RUN-END 不覆盖 lastEvent、lastMessage 保留「总计」、三口径自洽 todayTotal===各账号之和===RUN-END 总收集积分、跨天 RUN-END 不拖入次日）。验证：GUI 测试 157 通过 0 失败（unit 53 + api 81 + resilience 23，另有 7 项受限环境压缩用例跳过） |
| 2026-08-22 | **日志收益统计口径修复：按「运行段」聚合（多次运行漏算根因）**：`gui/lib/summary.js` 的 `generateSummary` 原按「(本地日期, 账号)」判定 `hasAccountEnd`——同一天同一账号多次运行（脚本重启/中断续跑，多次 ACCOUNT-START）时，只要最后一次运行有 ACCOUNT-END 就只取 END 总计，前面被截断运行的真实收益（仅存在于活动积分行）被整体丢弃（实测 guidata 日志 08-18 漏 59、08-19 漏 196，grandTotal 2050→2305）。改为按「运行段」聚合：ACCOUNT-START 为段边界，段内有 ACCOUNT-END 用 END 权威总计（防活动行与余额差口径不一致），段内无 END 用段内活动积分兜底；段收益归属段结束日（段内最后一行本地日期，兼容跨 UTC 日界运行）；段状态跨文件保持，并兼容无 ACCOUNT-START 的旧格式日志（惰性段按本地日期切分，避免独立运行误合并）。`daily`/`accountTotals`/`grandTotal`/`todayTotal` 结构不变。测试配套同步：`test/gui/helpers/sandbox.js` 夹具补 ACCOUNT-START 行（贴近真实日志格式，断言值不变）；`test/script/run-log-tests.js` 参考实现升级为段粒度口径（B6 改动态参考断言）。验证：guidata 日志 grandTotal=2305 与独立参考实现完全一致；GUI 测试 153 通过 0 失败（unit 49 + api 81 + resilience 23） |
| 2026-08-21 | **安装环境修复：GUI 侧注入干净 userconfig，非 GUI 文件与上游零差异**：npm 11.17 下 `npm run pre-build` 嵌套 `npm i` 会因用户级 `~/.npmrc` 的 `allow-scripts` 配置（开发环境注入，上游环境无此配置）误报 EALLOWSCRIPTS。`package.json`/`setup.bat` 保持与上游一致（不产生合并冲突），改为 `lib/routes/system.js` 的 `/api/setup` 在 spawn `setup.bat` 前：读 `~/.npmrc` → 剔除 `allow-scripts` 行 → 写入临时 userconfig → 注入 `NPM_CONFIG_USERCONFIG` 环境变量；点 GUI「安装环境」即可正常完成依赖安装，无需修改任何上游/根目录文件。验证：临时目录实测嵌套 `npm run` 注入干净 userconfig 后 `npm i` 通过（215 包）；GUI 测试 160 用例 153 通过 0 失败（新增 I-Y05 守护注入逻辑） |
| 2026-08-21 | **仪表盘日志呈现修复：状态一致性 + 布局重构 + 最新动态视觉增强（emil-design-eng 规范，测试驱动）**：①**状态一致性**——`js/app.js` 新增 `parseAccountEnd` 解析 ACCOUNT-END 消息关键字段（总计/原始→新值/持续时间），账号卡片「最新动态」按状态渲染完整状态块：完成态显示 ACCOUNT-END 圆角胶囊标签（`#F3F2F1`/`#E1DFDD`/`2px 8px`）+ 完整消息 + 「+XXX pts」「原始 X → 新值 Y」「总运行时间: Z 分钟」关键字段加粗主色 `#0078D4`；所有账号走同一渲染路径，末账号格式与前序账号严格对齐（此前仅一行文本，末账号完成标识易被忽略）；②**布局重构**——`design-reference.html` 把「任务实时日志」区块移至「账户总数/今日收益」汇总卡与首个账号卡片之间，形成「全局摘要 → 实时日志 → 账号列表」视觉动线；实时日志流中 ACCOUNT-END/RUN-END 里程碑行主色加粗高亮（`renderTaskLog`），收尾节点一眼可辨；③**视觉增强**——`css/main.css` 新增 `.latest-done`（高对比度边框 rgba(0,120,212,.35) + 微渐变背景，完成态静态呈现不附加无意义动画）、`.latest-running`（box-shadow 呼吸脉冲 2s，运行态指示活动，`prefers-reduced-motion` 降级）、`.account-end-pill`/`.key-point` 组件类；grid 响应式布局移动端/桌面端自适应（既有 md:grid-cols-2 断点）。测试：新增 R-F05（parseAccountEnd 解析正确性）、R-F06（布局动线顺序守护），159 用例 152 通过 0 失败；沙箱双账号浏览器实测：tester.a（+20 pts）与末账号 tester.b（+31 pts）状态块格式完全一致、日志区位于汇总卡与首账号之间、总收益 251 pts 口径无回归 |
| 2026-08-21 | **次要问题收尾：P2 清零 + 工程化补全（测试驱动，157 用例全绿、0 失败）**：①**D18 前端超时与轮询退避**——`js/app.js` `fetchJson` 加 `AbortSignal.timeout(15000)`（断网/无响应时 15s 主动 abort，不再永久挂起）；两处固定 `setInterval` 改为自调度 `setTimeout` + in-flight 锁 + 失败计数指数退避（任务 5s→10s→20s→40s→60s、数据 30s→60s→120s 封顶，成功立即重置计数，上一轮未完成则跳过本轮，锁在 finally 释放保证链路不断），断网/服务关闭时请求不再堆积——R-F03/R-F04 两个 P2 守护网用例由此转绿；**SSE 保活断线风暴**：浏览器内置 EventSource 自动重连间隔固定（~2.5s），服务关闭时疯狂重连刷屏（实测 Network 刷屏），改为 `onerror` 时主动 `close()` 阻止内置重连 + 手动 `setTimeout` 指数退避（5s→10s→20s→40s→60s 封顶，`onopen` 重置计数），60s 观察窗口内重连次数从 ~25 次降到 ~3 次；②**HTTP 服务超时**——`server.js` 补 `headersTimeout=20s / requestTimeout=60s / keepAliveTimeout=65s`（keepAliveTimeout 大于 headersTimeout；SSE 响应进行中不受约束）；③**日志读接口方法校验（同类未动项）**——`routes/logs.js` 的 `/api/logs/summary` 与 `/api/logs/:date` 补 `req.method !== 'GET' → 405`；顺手移除导出分支残留的 `Access-Control-Allow-Origin: *`（上轮 CORS 加固的漏网点）；④**archive 命令注入面**——`lib/archive.js` 不再把路径拼进 PowerShell `-Command` 字符串，改经 `GUI_ARCHIVE_SRC/DEST` 环境变量传递（临时目录含单引号不再中断命令或构成注入）；⑤**工程化**——`package.json` 新增 `"test": "node --test --test-isolation=none test/gui/*.test.js"`；新增 `lib/cleanup.js`：`.bak` 备份轮转（写前把旧 .bak 转存 `.bak.<UTC时间戳>`，每类保留最近 5 个，接入 accounts/config 全部写路径）与 cache 目录 7 天前文件清理（generateCache 后惰性触发），清理失败仅告警不影响主流程；`.gitignore` 补 `*.bak.*`、测试沙箱复制过滤器同步排除历史备份。测试：新增 I-L05b（summary/:date 405）、I-C16（bak 轮转≤5）、U-A04（archive 源码不得拼接路径）、U-C06（cache 7 天清理），总计 157 用例 150 通过 0 失败（7 跳过为受限环境压缩用例） |
| 2026-08-21 | **安全加固：本地 Token 鉴权 + 配置写并发锁 + 进程级单实例保护（闭合 TEST_REPORT 遗留风险 #1/#3，测试驱动）**：测试 140 → 153 用例（新增 13 个安全用例），通过 131 → 144，失败仍仅剩 2 项 P2 待办守护网（R-F03/R-F04）。①**无鉴权修复**——`gui/server.js` 启动时 `crypto.randomBytes(32)` 生成 256 位随机令牌，新增免鉴权接口 `GET /api/auth/token`，所有 `/api/*` 统一校验 `X-Auth-Token`（SSE 的 EventSource 无法自定义请求头，`/api/keepalive` 额外支持 `?token=`），不匹配返回 401；②**CORS 收紧**——`lib/httpUtils.js` `sendJson` 移除 `Access-Control-Allow-Origin: *`，跨站网页既读不到令牌接口响应也调不动任何接口（此前本机任意网页可读走全部账号凭据、启停任务）；③**凭据脱敏**——`lib/routes/accounts.js` GET 对 `password`/`totpSecret` 返回 `******`，PUT 将回传的脱敏占位符视为「未修改」剔除（否则前端编辑地理位置/代理等字段时会把占位符覆盖写入真实密码）；④**配置写互斥锁**——`lib/routes/config.js` 模块级 `isWriting` 锁 + 写入期间并发请求返回 409「系统正忙，请稍后重试」+ `finally` 释放；readBody 后 `setImmediate` 让出事件循环（本地回环小请求体与请求头同包缓冲时，若不让出，前一请求会在后一请求回调前完成并释放锁——实测 20 并发仍全部 200、最后写入者胜）；`reset` 接口未纳入互斥（手动触发，小面）。⑤**单实例保护**——`gui/server.js` 项目根 `.gui.pid` 存活检测（Windows 上 SO_REUSEADDR 允许重复 bind，端口检测会漏判，故选 pid 文件；EADDRINUSE 保留为 Linux/macOS 兜底），检测到存活实例友好提示退出，`exit` 钩子清理、强杀残留由下次启动兜底；`.gitignore` 补 `.gui.pid`。⑥**前端令牌集成**——`gui/js/app.js` 新增 `apiFetch` 统一封装（自动携带 `X-Auth-Token`、401 时提示并刷新页面）与 `fetchAuthToken`（页面加载先领取令牌，失败则提示并停止初始化），全部 19 处业务请求与 SSE 保活（`?token=`）改走令牌通道。测试基建同步：`test/gui/helpers/sandbox.js` 按 base 惰性缓存令牌自动注入请求头（`auth:false` 可关），`waitForServer` 改探测令牌接口；`api.test.js` I-P03 改为确定性占锁用例（不完整请求体占锁 → 19 并发全 409 → 中断后锁释放可再写），新增 I-SEC01~10（401/错令牌无副作用/令牌随机性/query token/CORS 头移除/脱敏/占位符不覆盖/回归守护）；`resilience.test.js` 新增 R-L01b（无令牌 keepalive 401）、R-L05（存活 pid 阻断启动）、R-L06（陈旧 pid 不阻断） |
| 2026-08-20 | **P1 健壮性与数据安全修复（7 项，测试驱动）**：140 用例通过数 126 → 138（剩余 2 项为前端 `fetchJson` 超时与轮询退避，属 P2 未排期）。①**账号字段边界**——`lib/validator.js` email 补长度 ≤254（RFC 5321）、禁空白/控制字符、禁 `< > " ' \` &`，proxy.port 由「有限数字」收紧为 0-65535 整数（此前 5000 字符邮箱、含换行或 HTML 片段的邮箱、port=-1/70000 都能落盘）；②**GUI 设置不再丢失**——`lib/config.js` `writeGuiSettings` 改为 `{ ...readGuiSettings(), ...settings }` 合并写入（原整体覆盖会清除 gui-settings.json 里的非端口设置）；③**配置字段白名单**——`routes/config.js` 新增 `ALLOWED_TOP_LEVEL`（源自 src/config.example.json 的 14 个顶层键），未知字段返回 400，避免任意键值经合并写回污染 config.json；已核对前端 `CONFIG_FIELD_MAP` 提交的 11 个顶层字段全部在白名单内；④**请求体读取收敛**——`lib/httpUtils.js` `readBody` 补 `aborted`/`close` 监听 + 30s 超时，并以 `settled` 标志保证只结算一次（客户端断网时原 Promise 永不 settle，已接收的请求体内存无法释放）；⑤**响应序列化容错**——`sendJson` 的 `JSON.stringify` 包 try/catch 并降级 500（循环引用等会让 res 永不 end、异常还会冒泡到分发层）；⑥**临时目录唯一性**——`lib/archive.js` `makeTmpRoot` 改用 `fs.mkdtempSync`（原 `Date.now()+pid` 拼接实测 2000 次仅 11 个唯一值，并发导入/导出会互相覆盖 zip 并删除对方目录）；⑦**统计层容错**——`lib/summary.js` `summarizeLogs` 改用 `const msg = e.message \|\| ''`，残缺条目不再抛 TypeError。浏览器实测确认：全局设置开关切换显示"✓ 已自动保存"且 `headless` 正确落盘（白名单未误伤保存链路）、端口改为 31500 后 `gui-settings.json` 仍保留 `theme: dark` |
| 2026-08-20 | **P0 缺陷修复（8 项，测试驱动）**：由 `test/gui/` 自动化基线定位的致命/严重缺陷全部修复，140 用例通过数 109 → 126（剩余 14 项属未排期的 P1/P2）。①**进程崩溃兜底**——`server.js` 分发层加 try/catch 并为异步路由 Promise 补 catch（异常统一转 500；此前路由内异常会成为 uncaughtException 直接终止 GUI），`lib/routes/accounts.js` GET 分支补数组结构与 email 类型校验（脏 accounts.json 曾使 `a.email.split` 抛错）；②**缓存故障降级**——`lib/logCache.js` `getCachedData` 包 try/catch 返回空摘要（缓存目录被占位/磁盘异常曾使 `/api/stats` 崩溃）；③**存储型 XSS**——`js/app.js` `escapeHtml` 四个 replace 的替换目标与源字符相同（HTML 实体被反解析）等于完全未转义，恢复为真实实体并补 `'`；账号卡片 `onclick="fn('${email}')"` 改 `data-email` + 事件委托（HTML 属性先实体解码再作 JS 解析，实体转义防不住注入，且含单引号的邮箱会让按钮彻底失效）；④**按日期查日志恒空**——`lib/logger.js` `readLogFile` 补 `.log` 后缀并加 `YYYY-MM-DD` 白名单（同时闭合 dateStr 目录穿越）；⑤**CRLF 日志统计归零**——`parseLogLine` 解析前剥离行尾 `\r`（JS 正则 `.` 不匹配 `\r`，导入的 Windows 日志会被整行丢弃）；⑥**任务并发竞态**——`lib/taskManager.js` 运行判定改 `exitCode/signalCode`、启动加 `starting` 互斥 + 3s 节流（5 并发启动曾有 2 个成功，且 `killed` 会把 SIGTERM 后仍存活的进程误报为已停止）；⑦**读方法产生副作用**——`routes/tasks.js`（start/stop/task）、`routes/system.js`（stats/summary/keepalive）、`routes/logs.js`（logs）补 `req.method` 校验返回 405（原先 GET 即可启停任务）；⑧**配置保存空引用**——`routes/config.js` 合并前提取 `current.searchSettings \|\| {}`。浏览器实测确认：XSS payload 以纯文本渲染、含单引号邮箱的"详细设置"按钮正常打开弹窗、页面零 JS 错误 |
| 2026-08-20 | **新增 GUI 自动化测试套件（`test/gui/`，被测代码零改动）**：因既有 `test/script/run-log-tests.js` 依赖被 .gitignore 排除的 `test/data/` 而无法在干净仓库运行，改以 `node:test` + tmp 沙箱副本重建可复现的测试基线。新增 `test/gui/helpers/sandbox.js`、`unit.test.js`、`api.test.js`、`resilience.test.js`、`coverage-report.js`。基线结果：140 用例 / 109 通过 / 31 失败，gui 后端行覆盖 82.3%、函数覆盖 94.1%。已定位待修缺陷（本次仅报告未改代码）：①`routes/accounts.js` GET 分支缺字段与数组防御 + `server.js` 分发无 try/catch → 脏 `accounts.json` 使进程崩溃（致命）；②`logCache` 写缓存无异常处理 → `/api/stats` 崩溃（致命）；③`js/app.js` 的 `escapeHtml` 四个 replace 替换目标与源字符相同（HTML 实体被反解析）→ 恒等函数，配合 innerHTML 渲染构成存储型 XSS（严重）；④`logger.readLogFile` 未补 `.log` 后缀 → `GET /api/logs/:date` 恒空（严重）；⑤`parseLogLine` 正则 `.` 不匹配 `\r` → CRLF 日志整行丢弃、统计归零（严重）；⑥`taskManager` 以 `killed` 作互斥 → 5 并发启动有 2 个成功（严重）；⑦`routes/tasks.js` 等未校验 `req.method` → GET 即可启停任务（严重）；⑧`routes/config.js:143` `current.searchSettings.chinaApi` 空引用（严重）。正面确认：zip slip 防护、Session 白名单、写前备份、并发账号写一致性、SSE 静默期与 shutdown 生命周期全部通过 |
| 2026-08-20 | **启动脚本重构：彻底消除 PowerShell 窗口（两种无感启动模式）**：①`start-gui.bat`（常规模式）——端口读取由 PowerShell 改为 `node -e`（node 必装、冷启动 ~200ms 远快于 PowerShell ~1s；JS 内避免 `\|&\<\>` 特殊字符以安全嵌入 for/f 反引号）；浏览器拉起由 `powershell -Command "Start-Sleep 3; Start-Process URL"` 改为 **CMD 原生 `start "" http://localhost:%PORT%`**（零 PowerShell 零黑框闪现）；硬编码 3 秒延迟改为 `ping -n 2 127.0.0.1` 约 1s 缓冲（不依赖 stdin，隐藏窗口/静默模式下 timeout 不可用，故弃用）；②`start-gui-silent.vbs`（静默模式）——保持 `shell.Run "start-gui.bat", 0, False` 隐藏 CMD，配合无 PowerShell 的 bat 实现**完全零窗口**；③`stop-gui.bat` 端口读取同步改为 node 并补 `cd /d "%~dp0"`。实测：常规模式端口正确显示（gui-settings.json=3006）、浏览器正常唤起；静默模式 cscript 执行成功、用户运行中的服务不受影响 |
| 2026-08-20 | **首次打开"环境安装"提示弹窗**：`gui/design-reference.html` 新增 `modal-env-setup`（完全复用既有 modal-root 模式：header 图标+标题+X、正文提示"请先安装环境，再执行其他操作"、"不再提示"复选框、btn-primary 确认按钮，250ms ease-out 进出动画 + reduced-motion 降级自动继承）；`gui/js/app.js` 新增 `ENV_SETUP_DISMISS_KEY` 常量 + DOMContentLoaded 内"勾选即写/取消即删 localStorage、无标记才 openModal"逻辑（浏览器实测：首次弹出 ✓、勾选确认后刷新不再弹 ✓）。浏览器 localStorage 持久化，无后端改动 |
| 2026-08-20 | **GUI 端口链路核查 + 文档同步**：全链路实测验证——GUI 保存端口（`PUT /api/gui-settings`）→ `gui/gui-settings.json` → start-gui.bat 动态读取注入 `PORT` → `lib/config.js` `resolvePort()` → server.js 监听与命令行显示一致（实测 gui-settings.json=3004 时启动日志显示 `http://localhost:3004`，含空格路径下 bat 解析亦正常）；代码无需改动（历史端口 3003 为旧版残留值，见 gui-settings.json.bak）。文档同步：目录表补 `gui/gui-settings.json`、接口表补 `GET/PUT /api/gui-settings`、start-gui.bat 与「端口配置」设计决策改为 gui-settings.json 动态读取描述 |
| 2026-08-20 | **CodeMap 全面同步**：gui 模块补全（lib/routes/ 8 路由拆行、summary.json、动效说明），"原有模块（参考）"扩写为完整 src/ 架构（browser/functions/interface/logging/util 五层），新增 scripts/ 与根目录配置小节，追加 GUI 前端动效规范设计决策 |
| 2026-08-20 | **GUI 前端动效优化（emil-design-eng / review-animations 规范落地）**：①弹窗进出对称动画——打开 opacity+scale(0.96)→1（250ms ease-out，`data-open` 属性 + 双 rAF 触发，替代原 10ms setTimeout 纯淡入），关闭先播退出动画再 display:none（transitionend + 300ms 兜底，`_closing` 防快速开关竞态）；②开关组件——滑块 `after:transition-all`→`after:transition-transform duration-200 ease-out`（消除 `transition: all` 硬性违规），轨道颜色补 `.toggle-track` 150ms ease 过渡；③收益进度条——`transition-all duration-500` + innerHTML 全量重绘（过渡永不生效、宽度瞬跳）改为 DOM diff 更新 + `transform: scaleX`（transform-origin:left，400ms ease-out，GPU 合成属性）；④面板切换 160ms 极轻淡入（tens/天高频克制档，不做过位移动画）；⑤图表动画——animator.js 首次生长 600ms→400ms、smoothUpdateChart 显式 `update({duration:400})`（原 Chart.js 默认 1000ms 拖沓）、reduced-motion 0ms；⑥`animate-ping`/面板/遮罩/保存提示/代理字段禁用态补齐 reduced-motion 或过渡；⑦日志自动滚动仅在用户接近底部时触发。涉及文件：`gui/css/main.css`、`gui/js/app.js`、`gui/js/animator.js`、`gui/design-reference.html` |
| 2026-08-19 | **新增日志导入/分析测试脚本**：`test/script/run-log-tests.js` 用 `test/data/logs-20260819-125022/`（logs-20260819-125022.zip 解压）跑 logger/summary 全流程测试。首次运行 31 项断言全部通过（解析 6551 行/过滤 555 异常行；每日收益 204/113/233/189/142/447/208；accountTotals 与独立参考实现完全一致）。期间测试脚本自身修过 3 处 bug（详见 commit 历史），被测代码零改动。验证了 08-14 同一天两次运行 ACCOUNT-END 累加 = 113、08-15/08-16 未完成账户走活动积分兜底（101/112）、08-19 无 ACCOUNT-END 兜底 208（2026-08-19） |
| 2026-08-19 | **仪表盘总览收益口径修正（累计收益 vs 账户余额）**：`gui/lib/summary.js` 修复收益统计两处 bug——①同一账号同一天多次运行 ACCOUNT-END `总计` 由"覆盖"改为"累加"（`summarizeLogs` 与 `generateSummary` 同步修正），此前只统计最后一次运行导致收益被低估；②统计日期由"UTC 时间戳切片"改为按本地时区换算（`toLocalDateKey`），修复跨 UTC 日界时"今日收益"偏差；`generateSummary` 新增 `todayTotal`（本地时区今日收益）。`gui/design-reference.html` 首页顶部收益卡标题改"今日收益 / 总收益"（原"今日总收益 / 总积分"右侧实为账户余额），统计页"今日收益"卡副标题同步更新。`gui/js/app.js`：新增 `statsCache`，`loadData` 并行拉取 `/api/stats`（30s 轮询已存在），`renderHome` 顶部卡改为读 `todayTotal`/`grandTotal`（脚本执行带来的收益，移除旧余额汇总逻辑），账号卡片"今日收益"改为今日多账号/多次运行累计；`renderStats` 优先用 `statsCache` 减少重复请求，今日收益改用后端 `todayTotal`。日志分析链路（`logger.js` 解析 → `summary.js` 聚合 → routes 分发）经核查拆分合理、无循环依赖，正则与 Logger.ts 输出格式完全匹配（2026-08-19） |
| 2026-08-19 | **GUI 按钮配色优化（设计系统化，方案 B）**：`gui/css/main.css` 新增按钮组件类（`.btn` 基础 + `.btn-dark` 启动任务深色 CTA / `.btn-primary` 蓝色主操作 / `.btn-danger` 红色紧急 / `.btn-secondary` 白底描边次级 / `.btn-danger-ghost` 红字描边次级危险 / `.btn-ghost` 中性描边取消 / `.btn-icon` 图标按钮）；统一按压反馈 `:active scale(0.97)`（emil-design-eng 原则）、`:focus-visible` 焦点环（WCAG 2.4.7）、hover 加深、`prefers-reduced-motion` 降级；`gui/design-reference.html` 约 22 处静态按钮与 `gui/js/app.js` 账号卡片 2 处动态图标按钮全部替换为组件类；`app.js` 中 3 处依赖旧类名的 JS 选择器同步更新（`#panel-settings .btn-danger-ghost` / `#modal-add-account .btn-primary` / `#modal-account-settings .btn-primary`）。设计决策：实心灰次级按钮→白色描边按钮（降低层级，突出蓝色主操作）；红色等重→分级（停止任务=红色实心紧急操作，关闭服务/重置默认=红色描边可逆操作）（2026-08-19） |
| 2026-08-19 | GUI 全局设置改为**即时保存**：移除右上角"保存全部配置"按钮；`gui/js/app.js` 新增 `CONFIG_FIELD_MAP` 字段映射表 + `saveConfigSilent` 增量提交（checkbox 立即保存、text 输入 500ms 防抖）+ 串行链防并发乱序 + 右上角"已自动保存/保存失败"状态提示；成功后用后端合并结果更新 `configCache` 避免 30s 重渲染覆盖；后端 `PUT /api/config` 本身是合并写回，零后端改动（2026-08-19） |
| 2026-08-19 | `gui/design-reference.html` 仪表盘标题区（"仪表盘总览 / 账户汇总与任务运行状态"）距顶部常驻栏距离与其他页面统一：根因是 `#contentPanels` 的 `space-y-8` 仅对非首个面板生效，导致仪表盘（首个 `panel-home`）少了 32px；修复为给 `panel-home` 加 `mt-8`，与其他页面统一为 64px |
| 2026-08-19 | `gui/design-reference.html` 导入/导出图标方向统一：**导出朝上（`m-4-8l-4-4m0 0L8 8m4-4v12`）、导入朝下（`m-4-4l-4 4m0 0L8 12m4 4V4`）**。涉及仪表盘数据、Session、日志三组六个按钮 |
| 2026-08-19 | `start-gui.bat` 修复：曾试 `chcp 65001`，但代码页切换期缓冲错位仍会啃掉后续中文行（报 `'澶勶紙server.js'`），最终**改为纯 ASCII**（英文注释/提示），任何代码页解析一致，从根上消除乱码；端口统一为 3000（start-gui / stop-gui / 文档同步）。`start-gui-silent.vbs` 注释改纯 ASCII，与编码无关 |
